# Full version of vim (rather than, for example, vim-tiny)
# sudo apt -y install vim

# Also install gvim
# sudo apt -y install vim-gnome

# gvim with ruby
# sudo apt remove vim-gnome
#
# Well, the following still does not seem to install ruby with gvim
sudo apt -y install ruby rubygems vim-nox

